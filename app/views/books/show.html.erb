<p id="notice"></p>
<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being saved:
  <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>
<div class="container px-5 px-sm-0">
 <div class="row">

<%= render 'books/info', user: @user %>


<div class="col-md-8 offset-md-1">
 <h2>Book detail</h2>
  <table class="table">

   <tbody>
     <tr>
       <td>
         <img class="attachment user profile_image fallback" src="/assets/no-image-icon-d3f9e11cb2bb284b42e0810f94b03c50126f51f482ea43054b87c2b7ebaa01d5.jpg"><br>
            <%= @book.user.name %>
        </a>
       </td>
       <td>
        <%= link_to @book.title, book_path(@book.id) %>
       </td>
       <td>
         <%= @book.body %>
       </td>
       <% if @book.user == current_user %>
        <td><%= link_to "Edit", edit_book_path(@book), "class" =>"btn btn-sm btn-success" %></td>
        <td><%= link_to "Destroy", books_path, method: :delete, "data-confirm" => "本当に削除しますか？", "class" => "btn btn-sm btn-danger" %></td>
       <% end %>
      </tr>
    </tbody>

   </table>

 </div>
 </div>
 </div>
